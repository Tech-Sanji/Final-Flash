# Copy this file to .env.local and fill in your values

# SQLite database (auto-created in prisma/ folder — no setup needed)
DATABASE_URL="file:./prisma/dropzone.db"

# Anthropic API key for ARIA AI assistant (optional — fallback works without it)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
